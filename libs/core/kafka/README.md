# core/kafka

Reusable NestJS Kafka infrastructure.

Import it with:

```ts
import { KafkaModule, KafkaService } from '@kafka-base-implementation/core/kafka';
```

## Running unit tests

Run `nx test kafka` to execute the unit tests via [Jest](https://jestjs.io).
